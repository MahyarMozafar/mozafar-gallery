# --- secrets: NEVER commit these ---
.env
.env.*
!.env.example

# --- uploaded product photos (keep the folder, not the files) ---
/public/uploads/*
!/public/uploads/.gitkeep

# --- customer data: bank receipts show card numbers ---
/private-uploads
/receipts

# --- database ---
# خود پایگاه داده و همه فایل‌های جانبی‌اش.
# فایل -wal محتوای واقعی جدول‌ها را دارد — شماره موبایل مشتری و توکن نشست.
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
/data/

# --- build / deps ---
/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts

# --- os / editor ---
.DS_Store
*.log
npm-debug.log*
.vscode
.idea
